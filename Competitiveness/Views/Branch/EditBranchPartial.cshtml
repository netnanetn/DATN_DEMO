@model Competitiveness.Branchs
<div class="row">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.BranchId)

        <div class="panel panel-default">
            <div class="panel-heading">
                <h4 class="panel-title">Chỉnh sửa Ngành cần xây dựng bộ dữ liệu cạnh tranh @Model.BranchName</h4>
            </div>
            <div class="panel-body">
                <form role="form">
                    <div class="form-group">
                        <label class="control-label" for="">Tên ngành</label>
                        <div class="controls">
                            @Html.EditorFor(model => model.BranchName, new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                    </div>
                </form>
            </div>
            <div class="panel-footer">
                <div class="form-group">
                    <button class="btn btn-primary" type="submit">Lưu</button>
                    <button class="btn btn-default" style="float:right" id="btnClose">
                        Hủy
                    </button>
                </div>
            </div>
        </div>
    }

</div>


